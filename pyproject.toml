[project]
name = "hermes-slicer"
version = "0.1.0"
description = "Local/private Hermes floating panel and OrcaSlicer bridge."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
